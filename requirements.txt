# Django Core (Actually Used)
Django==5.2.4
djangorestframework==3.15.0
djangorestframework-simplejwt==5.3.0
django-cors-headers==4.3.1

# Database (Currently using SQLite, but PostgreSQL for production)
psycopg2-binary==2.9.9

# Image Processing (For future profile pictures, medicine images)
Pillow==10.1.0

# Environment Variables
python-decouple==3.8

# HTTP Requests (For API calls)
requests==2.31.0

# Development Tools (Optional but recommended)
django-debug-toolbar==4.2.0

# Production Server
gunicorn==21.2.0

# Static Files (For production)
whitenoise==6.6.0
